# declare -A obj_properties
# obj_properties=()

# properties IDs
fileName=0
fileSize=1

obj.method1(){
    echo obj method1 called
}

obj.sayHello(){
    echo Hello!
}

obj.property(){
    if [[ "$2" == "=" ]]
    then
        obj_properties["$1"]="$3"
    else
        echo ${obj_properties["$1"]}
    fi
}

obj.fileName(){
    if [[ "$1" = "=" ]]
    then
        obj.property fileName = "$2"
    else
        obj.property fileName
    fi
}

obj.folderName(){
    if [ "$1" == "=" ]
    then
        obj.property folderName = "$2"
    else
        obj.property folderName
    fi
}

obj.posx(){
    if [[ "$1" == "=" ]]
    then
        obj.property posx = "$2"
    else
        obj.property posx
    fi
}

obj.posy(){
    if [[ "$1" == "=" ]]
    then
        obj.property posy = "$2"
    else
        obj.property posy
    fi
}